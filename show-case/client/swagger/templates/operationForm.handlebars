{{#if requestBody}}
   {{methodName}}FormGroup(value?:{{{requestBody.type}}}) {
   {{#isRef requestBody.spec.schema }}
     let schema: any =  {{{requestBody.type}}}Schema;
   {{else}}
     let schema: any = {{{json requestBody.spec.schema}}}
   {{/isRef}}
    return this.getFormGroup<{{{requestBody.type}}}>(schema, value);
  }

   {{methodName}}FormModel(options?: Partial<FormModel> & { displayProperties?: (keyof {{{requestBody.type}}} & string)[] }, value?: {{{requestBody.type}}}) {
    const schema: any = UserSchema;
    return this.getFormModel<{{{requestBody.type}}}>(schema, options, value);
  }
{{/if}}
